{
	"name": "Reverse Proxy",
	"description": "Spin up a reverse proxy quickly on Heroku",
	"keywords": [
		"reverse proxy",
		"nginx"
	],
	"repository": "https://github.com/ServerSMP-Github/heroku-reverse-proxy",
	"buildpacks": [
		{
			"url": "https://github.com/heroku/heroku-buildpack-nginx.git"
		}
	],
	"env": {
		"UPSTREAM_SERVER": {
			"description": "URL of upstream server to which all requests should be proxied"
		}
	},
	"stack": "heroku-20"
}
